Check which process is using a port: sudo ss -lptn 'sport = :3100'
Run the Node server in background (even when no shell is open): nohup sudo node index.js &
Run the Rasa NLU server in background (even when no shell is open): nohup sudo python3.5 -m rasa_nlu.server -c config-spacy.json &

python -m rasa_core_sdk.endpoint --action action1
python -m rasa_nlu.server --path models --pre_load default/model_name -c config.yml
python -m rasa_core.run -d model/dialogue -u model/current/nlu --endpoints HRbot/endpoints.yml
python -m rasa_core.train -s HRbot/stories.md -d HRbot/domain.yml -o model/dialogue